{
  "name": "xabier-iribar-commercial-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "astro dev",
    "og:generate": "node scripts/generate-og.mjs",
    "build": "npm run og:generate && astro build && node scripts/create-sitemap-alias.mjs",
    "preview": "astro preview",
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "check": "astro check",
    "test": "npm run build && node --test tests/site.test.mjs",
    "audit:lighthouse": "mkdir -p tmp && start-server-and-test 'astro preview --host 127.0.0.1 --port 4321' http://127.0.0.1:4321 'lighthouse http://127.0.0.1:4321/ --quiet --output=json --output-path=./tmp/lighthouse.json --only-categories=performance,accessibility,best-practices,seo --form-factor=mobile --screenEmulation.mobile=true --chrome-flags=\"--headless --no-sandbox\"'"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.4.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@tailwindcss/vite": "^4.3.0",
    "lighthouse": "^13.4.0",
    "prettier": "^3.8.3",
    "prettier-plugin-astro": "^0.14.1",
    "start-server-and-test": "^3.0.11",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.9.3"
  }
}
